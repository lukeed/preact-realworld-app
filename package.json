{
  "private": true,
  "name": "realworld",
  "version": "0.0.0",
  "repository": "lukeed/preact-realworld-app",
  "license": "MIT",
  "scripts": {
    "build": "preact build --template src/index.html",
    "serve": "preact build --template src/index.html && preact serve",
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "watch": "preact watch --template src/index.html"
  },
  "devDependencies": {
    "if-env": "^1.0.0",
    "preact-cli": "^2.0.0"
  },
  "dependencies": {
    "history": "^4.7.2",
    "mitt": "^1.1.2",
    "preact": "^8.2.1",
    "preact-router": "^2.5.5",
    "snarkdown": "^1.2.2",
    "unfetch": "^3.0.0"
  }
}
